# 项目开发规范

## 产品边界

本仓库构建一个 Chrome Manifest V3 扩展：用户在 `chat.deepseek.com` 或 `chat.z.ai` 的 Chat 模式正常提问时，扩展通过可见的 Harness，让网页大模型按需读取用户主动导入的本地 Skill 说明和 Reference、调用用户添加的 MCP 服务，并自动形成最终回答。

首版只支持以下能力：

- 浏览器仅限 Google Chrome 桌面版，目标网站仅限 `chat.deepseek.com` 和 `chat.z.ai` 的 Chat 模式。
- 用户可导入、查看、覆盖和删除包含 `SKILL.md` 与可选 `references/` 的 Skill 包。
- 用户可添加、查看和删除无鉴权 Streamable HTTP MCP 服务；模型读取服务详情后，默认须经用户确认才可调用 MCP Tool，用户也可信任当前目标网站会话中的该服务，免于重复确认。
- 用户可全局启用或停用 Skill 功能；停用后仍进入固定的对话式 Harness 流程，但 Harness 只提供 MCP 能力，不披露 Skill、Reference 或相关会话知识。
- 用户可配置 `1` 至 `60` 秒的自动回注延迟闭区间；扩展在每次适用的回注前独立抽样一个整数秒。
- 所有用户消息默认进入同一种固定的对话式 Harness 流程，任务开始时读取的设置决定本次使用完整 Harness 还是 MCP-only Harness。
- Harness 只解析正文为严格 YAML mapping 的显式围栏命令块：`skill` 用于读取 Skill 说明，`read` 用于读取该 Skill 的 Reference，`mcp` 用于读取服务详情，`mcp-call` 用于调用已披露的 Tool。
- 模型可批量请求 Skill，再批量请求 Reference；扩展校验、读取并回注内容后等待最终回答。
- Skill 和 Reference 读取无需逐次审批；普通模式和深度思考模式都只解析最终回答区域。

除非用户明确批准变更产品范围，否则不得加入以下能力：

- 其他浏览器、其他网站、z.ai Agent 模式、网页侧栏、Chrome Side Panel 聊天界面或自建对话框。
- 任意本地目录访问、文件写入、写入审批、差异预览、全文搜索或二进制资料读取。
- 任务恢复、跨标签页接管、租约、心跳或持久化检查点。
- 多套可插拔协议、运行时自定义 Harness、多项目会话或多智能体行为。
- 向量索引、Embedding、语义检索或通用本地知识库能力。

## 核心术语

- **Skill 包**：用户导入的 ZIP；包含一份 `SKILL.md` 和可选的 `references/` 文本资料。
- **Skill**：以名称唯一标识的一组模型使用说明及其可选 Reference，不称为提示词、模板或 Agent。
- **Reference**：属于某个 Skill、位于 `references/` 下并按需读取的文本资料，不代表任意本地文件。
- **Skill 目录**：当前已导入 Skill 的名称和描述清单，不包含正文。
- **Harness**：扩展随用户问题发送的固定协作约定，包含命令语法、MCP 服务目录和用户原始问题；Skill 功能启用时还包含 Skill 目录及 Skill、Reference 协作信息。它不是可插拔协议或隐藏系统消息。
- **MCP 服务**：用户主动添加并允许扩展在增强任务中使用的外部 MCP 能力来源。
- **MCP Tool**：由某个 MCP 服务公开、可在增强任务中按名称和结构化参数调用的一项能力。
- **Agent Loop**：增强任务内由网页大模型显式请求 Skill、Reference 或 MCP，扩展校验并回注真实内容后，模型继续处理直至形成最终回答的协作循环；不代表多智能体行为。
- **增强任务**：由一条用户消息发起的临时问答流程；活动状态只存在于当前页面内存中。
- **自动回注**：扩展识别网页大模型的显式命令后，自动发送的 Skill、Reference、MCP 内容或纠正反馈；首次 Harness 不属于自动回注。
- **最终回答**：网页大模型未包含任何已识别命令的回复；该回复结束当前增强任务。

## 开发流程

### 分支流程

- 开发分支必须从 `main` 创建。
- 代码开发和单元测试在开发分支进行。
- 开发分支只能合并到版本分支。
- 版本分支必须从 `main` 创建，并可合并多个开发分支。
- 集成测试在版本分支进行。
- 当前主分支为 `main`。

每个需求按以下顺序交付：

1. 每个需求只定义一种用户可观察行为，并在实现前明确行为、架构取舍和验收标准，取得用户确认。
2. 列出实现步骤和明确的验证方式。
3. 只实现当前需求及其必要测试。
4. 运行自动化验证，并完成对应的真实 Chrome 验收场景。
5. 检查实际输出并保留验收结果。
6. 提交当前需求后，才能开始下一个需求。

当前里程碑存在未验证行为时，不得开始后续里程碑。不得为假设中的第二个网站、协议、浏览器、命令或任务生命周期提前抽象；只有第二个已批准的具体用例证明存在共享边界后，才可引入抽象。

## 架构规则

- DeepSeek 和 z.ai DOM 相关知识只能存在于各自的站点适配器中。
- 活动增强任务只保存在当前页面内存中；仅持久化 Skill、配置、MCP 设置及目标网站会话范围内的工具知识和信任状态。
- Skill 功能状态在增强任务开始时确定；停用时不得向 Harness 提供 Skill、Reference 或相关会话知识，后台也必须拒绝目标网站发起的 Skill 和 Reference 读取。
- 自动回注延迟必须在实际发送前读取最新设置并支持任务取消；首次 Harness 和本次实际弹出 MCP Tool 确认框后的反馈不延迟。
- 扩展 Service Worker 必须保持面向请求，不得成为长期运行的任务协调器。
- 只解析显式围栏命令块，不得从自然语言意图推断命令。
- 读取任何一项前，必须先完整校验整个读取批次。
- 导入的 Skill 内容视为用户信任的模型指令，但绝不能作为浏览器代码或操作系统代码执行。
- `web-llm-enhance` 只可作为经验、测试夹具和已观察行为的只读来源，不得默认复制其架构。

## 真实 DOM 取证

- 不得根据记忆、截图、旧选择器或对页面框架的猜测编写目标网站 DOM 逻辑。
- 新增或修改选择器、输入框交互、生成状态检测、最终回答提取或 DOM 测试夹具前，必须使用 `$ego-browser` 检查目标网站的真实 DOM 和行为。
- 先用 `snapshotText()` 观察语义结构；需要精确 DOM 关系或浏览器状态时，再使用 `js()` 或 CDP。发生重要交互后必须重新观察，不能把交互前的状态当作验证结果。
- 在真实页面验收中，触发页面点击后应立即观察 DOM。若 DOM 未变化，应依据已观察到的页面状态判断是否进入正常流程，再决定等待或继续操作。
- 判断目标网站是否仍在回复时，必须读取输入框、发送或停止控件以及最新消息节点等 DOM 状态，不得只根据脚本返回值推断。
- 每次取证必须留存目标 URL 形态、页面模式、观察日期、关键 DOM 证据，以及据此采用的稳定选择器或结构不变量。
- 优先使用稳定的语义属性和已验证的结构不变量。仅在真实页面证明没有更强锚点时，才可使用生成类名，并须记录其局限。
- 只有基于 `$ego-browser` 观察到的真实页面，才可创建或更新 DOM 测试夹具；提交前必须清理其中的用户内容。
- 无法检查真实页面时，停止相关 DOM 开发并报告缺失的证据，不得实现猜测性降级方案。

## 代码注释

- 注释的语言必须为简体中文。
- 每个导出函数上方都必须添加 JSDoc，说明用途、重要输入和输出，以及存在时的副作用。
- 非导出函数仅在名称不足以表达意图或逻辑不直观时添加注释。
- 一个函数包含多个业务阶段时，使用 `Step 1`、`Step 2`、`Step 3` 等注释标记阶段边界。
- Getter、类型守卫、简单映射和单行包装函数无需注释。
- 注释必须解释意图、约束或执行顺序，不得复述单条代码语句。

## 验证规则

- ZIP 校验、协议解析与格式化、设置存储、自动回注延迟、任务状态转换和站点 DOM 行为必须分别测试。
- 站点适配器测试使用已保存的 DOM 测试夹具，以保证结果确定。
- 测试夹具只用于回归验证，不能替代 `$ego-browser` 的真实页面检查。
- `scripts/acceptance/real-browser-harness.sh` 是本仓库通用的真实 Chrome 验收 SOP；所有真实验收必须通过该脚本中已经沉淀的场景执行，临时浏览器脚本或手工操作只能用于取证和排障，不能作为最终验收结果。
- 当前 SOP 不存在对应场景时，必须先把场景、前置数据、断言和数据恢复流程沉淀到 `scripts/acceptance/real-browser-harness.sh`，验证脚本本身后再执行真实验收；不得绕过 SOP 直接宣布验收通过。
- 每个完成的里程碑都必须保留真实 Chrome 验收结果；自动化测试不能替代真实验收。
- 未运行测试、未检查预期输出，或里程碑要求的真实浏览器行为尚未验证时，不得将任务视为完成。
