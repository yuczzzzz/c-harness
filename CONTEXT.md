# c-harness

本上下文定义一个使用本地 Skill 增强网页大模型问答的个人 Chrome 扩展。它只管理被用户主动导入的 Skill，不代表通用本地知识库或文件系统代理。

## 语言

**Skill 库**：
用户导入并由扩展保存在本机的 Skill 集合。
_避免_：本地知识库、沙盒、工作区

**Skill 包**：
一个可导入的 ZIP，其中包含一份 `SKILL.md` 和可选的 `references/` 支撑资料。
_避免_：上传文件、插件包、知识库

**Skill**：
以名称唯一标识的一组模型使用说明及其可选 Reference。
_避免_：提示词、模板、Agent

**Skill 功能**：
控制 Harness 是否向网页大模型提供 Skill 和 Reference 协作能力的全局功能；停用不改变 Skill 库中已保存的内容。
_避免_：单个 Skill 状态、Skill 激活

**Reference**：
属于某个 Skill、在 `references/` 下保存并按该 Skill 使用说明按需读取的文本资料。
_避免_：任意本地文件、附件、文档库

**Skill 目录**：
当前 Skill 库中所有 Skill 的名称和描述清单，不包含 `SKILL.md` 或 Reference 正文。
_避免_：文件目录、索引、检索结果

**Harness**：
扩展随用户问题发送给网页大模型的固定协作约定，其中包含当前启用功能的目录、显式围栏 YAML 命令语法和当前问题。
_避免_：系统消息、可插拔协议、隐藏提示词

**MCP 服务**：
用户主动添加并允许扩展在增强任务中使用的外部 MCP 能力来源。
_避免_：插件、Skill、Agent

**MCP Tool**：
由某个 MCP 服务公开、可在增强任务中按名称和结构化参数调用的一项能力。
_避免_：命令、Skill、函数插件

**Agent Loop**：
增强任务内由网页大模型显式请求 Skill、Reference 或 MCP，扩展校验并回注真实内容后，模型继续处理直至形成最终回答的协作循环。
_避免_：多智能体、后台代理、任务恢复

**MCP 服务目录**：
当前已添加 MCP 服务的名称和描述清单，不包含服务地址、Instructions 或 MCP Tool 详情。
_避免_：Tool 列表、服务注册表、MCP 市场

**会话工具知识状态**：
某个目标网站会话中已经成功提供给网页大模型的 Skill 和 Reference 内容记录。该状态跨页面导航和刷新保留；对应内容发生变化或被删除后失效。它不包含工具轮次或进行中的增强任务。
_避免_：任务状态、文件缓存、对话恢复

**会话 MCP 信任状态**：
用户针对某个目标网站会话授予的 MCP 服务调用许可；重新打开同一会话时仍然有效，但不适用于其他会话。
_避免_：全局信任、永久授权、单次调用许可

**会话 MCP 详情状态**：
某个目标网站会话中已经提供给网页大模型的 MCP 服务详情记录；服务详情变化或服务被删除后失效。
_避免_：MCP 响应缓存、连接状态、全局 Tool 目录

**增强任务**：
由一条用户消息发起的临时问答过程，可以按显式命令完成多轮 Skill、Reference 或 MCP 交互，并以最终回答结束。
_避免_：Agent 会话、任务循环、后台作业

**Skill 请求批次**：
网页大模型在一条回复中通过多个 `skill` 命令请求的全部 `SKILL.md`。
_避免_：Skill 上传、Skill 激活、单项工具调用

**Reference 请求批次**：
网页大模型在一条回复中通过多个 `read` 命令请求的全部 Reference。
_避免_：文件读取批次、目录扫描、全文搜索

**最终回答**：
网页大模型未包含任何已识别命令的回复；它结束当前增强任务。
_避免_：完成事件、无工具回复

**目标网站**：
受到扩展增强的网页大模型网站，当前包括 `chat.deepseek.com` 和 `chat.z.ai` 的 Chat 模式。
_避免_：Agent 页面、模型后端、通用站点
