# c-harness 隐私政策

生效日期：2026 年 7 月 31 日  
最近更新：2026 年 8 月 5 日

c-harness（以下简称“本扩展”）是一款浏览器扩展，用于在受支持的网页大模型对话中，按需提供用户主动导入的 Skill、Reference，以及用户主动添加的 MCP 服务能力。本政策说明本扩展处理哪些数据、这些数据如何使用和保存，以及用户如何删除这些数据。

本扩展的开发者为 yuczzzzz。使用本扩展即表示相关数据还可能受到用户所选择网页大模型服务自身隐私政策和服务条款的约束。

## 1. 适用范围与单一用途

本政策适用于 c-harness 扩展本身。当前版本仅在以下网站运行：

- `https://chat.deepseek.com/`
- `https://chat.z.ai/`

本扩展的单一用途是：在用户正常使用受支持网页大模型时，通过可见的 Harness 协作约定，让模型按需使用用户主动提供的本地 Skill、Reference 和 MCP 服务。

## 2. 本扩展处理的数据

本扩展可能处理以下数据：

### 2.1 Skill 和 Reference

当用户主动导入 Skill ZIP 包时，本扩展会在本地读取并校验 ZIP，保存其中的：

- `SKILL.md` 的文本内容；
- `references/` 目录下符合要求的文本内容；
- Skill 名称、描述、Reference 数量、文件大小和导入时间等管理信息。

ZIP 中不属于上述范围的文件不会作为 Skill 内容保存。Skill ZIP 的读取、校验和保存过程不会由本扩展发送到开发者服务器。

### 2.2 网页对话内容与会话信息

为执行增强流程，本扩展会在受支持网站中读取和处理：

- 用户准备发送的问题；
- 网页大模型返回的回复，包括显式 Skill、Reference 或 MCP 命令和最终回答；
- 当前对话 URL 中用于区分会话的标识；
- 输入框、发送或停止控件、最新消息等与判断对话状态有关的页面信息。

这些信息用于组织当前增强任务、判断模型是否请求用户提供的内容或 MCP 能力、执行明确命令并等待最终回答。本扩展不会将这些信息用于分析用户画像、广告或其他无关用途。

### 2.3 MCP 服务

当用户主动添加 MCP 服务时，本扩展会在本地保存：

- MCP 服务地址及其权限来源、服务 ID、名称、版本、描述、Tool 数量、协议版本、详情摘要、检测状态和相关时间；
- 目标网站会话范围内已经披露过的 MCP 服务详情摘要；
- 用户为当前目标网站会话确认过的 MCP Tool 调用信任状态。

在添加、重新检测、披露服务详情或调用 Tool 时，本扩展会连接相应 MCP 服务，并临时读取或处理服务 instructions、Tool schema、Tool 名称、结构化参数和返回结果。完整 instructions 和 Tool schema 不作为 MCP 服务记录持久保存；Tool 调用参数和返回结果只保留在当前增强任务内存及发送后的网页对话中。

MCP 服务目录会随 Harness 作为可见对话消息发送给当前网页大模型。服务详情只会在模型通过显式命令请求后发送。MCP Tool 调用默认要求用户确认；用户可以仅允许本次调用，也可以信任当前目标网站会话中的该服务。建立会话信任后，只要服务详情未变化，后续调用不再重复确认。调用结果会作为可见对话消息发送给当前网页大模型。

请不要导入包含不希望交由所选网页大模型处理的密码、密钥、身份信息、商业秘密或其他敏感内容的 Skill 或 Reference；也不要添加会返回此类内容的 MCP 服务。

### 2.4 通用设置

本扩展会在本地保存用户配置的 Skill 功能开关，以及自动回注延迟的最小值和最大值。这些设置用于决定 Harness 是否提供 Skill 和 Reference 能力，以及控制扩展自动回注前的等待时间，不会发送给开发者服务器或网页大模型。

### 2.5 本扩展不主动收集的数据

本扩展不提供分析或遥测功能，不主动收集完整浏览历史、广告标识符、支付信息、健康信息、精确位置、通讯录、账户密码或网站 Cookie。本扩展也不使用这些数据进行广告投放、信用评估或用户画像。

## 3. 数据何时会发送给第三方

本扩展通过受支持网站自身的输入框和发送流程，将增强任务作为用户可见的普通对话消息发送。根据使用方式，消息可能包含：

- 用户原始问题和固定 Harness 约定；
- Skill 功能启用时，已导入 Skill 的名称和描述清单；
- 网页大模型明确请求的 `SKILL.md` 或 Reference 正文；
- MCP 服务目录、模型明确请求的 MCP 服务详情；
- 用户当次允许或已通过当前会话信任授权的 MCP Tool 参数和返回结果。

这些内容会由用户当前使用的 DeepSeek 或 Z.ai 网页服务接收，并可能按照相应服务的隐私政策、账户设置和保留规则保存或处理。开发者不控制这些第三方服务。用户从本扩展删除本地数据，不会自动删除已经进入 DeepSeek 或 Z.ai 对话历史的内容；用户需要使用相应网站提供的对话删除或账户管理功能处理这些副本。

除上述由用户使用网页大模型而发生的传输外，本扩展当前不连接开发者运营的服务器，也不把数据发送给开发者。本扩展不出售、出租或共享用户数据给广告商或数据经纪商。

## 4. 本地存储与保留期限

本扩展使用浏览器本地存储保存以下数据：

- **扩展本地数据：** 已导入 Skill 的管理信息、`SKILL.md` 和 Reference 文本；已添加 MCP 服务的配置和检测摘要；按目标网站来源与会话标识区分的 MCP 服务详情披露和信任状态；Skill 功能开关和自动回注延迟设置。
- **目标网站本地数据：** 会话标识，以及已提供 Skill 或 Reference 的名称、内容摘要和更新时间。内容脚本使用的 IndexedDB 归属于对应目标网站的站点存储。
- **页面内存：** 当前增强任务的状态、模型命令和工具结果。任务结束、页面关闭或刷新后，这类临时状态不作为任务检查点保留。

本扩展不会把上述本地数据同步到开发者服务器。除非数据被覆盖、被判定失效、用户主动删除，或浏览器因自身存储策略清理，否则当前版本不设置固定的自动过期时间。

## 5. 如何删除数据或撤销权限

用户可以通过以下方式控制数据：

- 在扩展的“Skill 管理”页面删除某个 Skill；该操作会删除该 Skill 的管理信息、`SKILL.md` 和全部 Reference。
- 在扩展的“MCP 管理”页面删除某个 MCP 服务；该操作会删除该服务配置、相关披露记录和信任状态。若没有其他已添加服务使用同一来源，扩展还会撤销对应的 MCP 主机权限。
- 卸载本扩展，以移除由扩展自身保存的数据；用户也可以通过浏览器提供的扩展数据管理功能清理相关数据。
- 在浏览器的站点数据设置中清除 `chat.deepseek.com` 或 `chat.z.ai` 的站点数据，以删除本扩展保存在相应站点 IndexedDB 中的 Skill 和 Reference 会话知识。清除站点数据也可能同时删除该网站自身保存的 Cookie、登录状态和其他站点数据。
- 在 DeepSeek 或 Z.ai 中删除相关对话，以请求删除已经发送给该服务的对话副本；实际处理结果和保留期限由相应服务决定。

## 6. 权限说明

本扩展请求访问 `chat.deepseek.com` 和 `chat.z.ai`，仅用于在这些页面中读取必要的对话状态、拦截用户主动发起的增强问题、发送可见 Harness 和工具结果。

本扩展会在用户添加或重新检测 MCP 服务时，请求该服务来源对应的主机权限。未授予主机权限时，本扩展不能连接该 MCP 服务；删除最后一个使用该来源的 MCP 服务时，扩展会撤销对应权限。

## 7. 数据安全与 Limited Use

本扩展将 Skill 包内容视为文本，不会把导入内容作为浏览器代码或操作系统代码执行。它会校验 Skill ZIP，并限制可保存和回注的文本范围。

本扩展仅为向用户提供上述单一用途而使用相关数据。数据不会用于个性化广告、再营销、数据经纪、信用或借贷决策，也不会由人工读取，除非用户主动在公开问题或支持请求中提供相关内容，或法律要求开发者处理。

尽管本扩展采取了范围限制和本地处理措施，任何软件和网络传输都无法保证绝对安全。用户应根据所选网页大模型服务的隐私规则，判断是否适合发送具体内容。

## 8. 政策变更

如果本扩展的数据处理方式、支持网站或功能范围发生实质变化，本政策将更新生效日期和相关说明。重大变更会在适当的发布说明或项目页面中告知用户。

## 9. 联系方式

如对本隐私政策或数据处理有疑问，请通过项目的 GitHub Issues 联系开发者：

<https://github.com/yuczzzzz/c-harness/issues>
